"""Application layer – orchestrates use-case business rules.

The package will gradually be populated with small command / query handlers
(backed by domain entities) as we migrate logic out of the bulky *service*
modules.
"""
