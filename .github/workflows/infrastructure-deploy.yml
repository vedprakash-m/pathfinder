# DEPRECATED WORKFLOW: All infrastructure deployment logic is now embedded in ci-cd-pipeline.yml
name: _Deprecated Infrastructure Deploy (No-Op)

# No automatic triggers
on: workflow_dispatch

env:
  NOTE: "This workflow is kept for historical reference only and does nothing."

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is deprecated. Use main CI/CD pipeline instead."
